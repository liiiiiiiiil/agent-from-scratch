# 治理文档（governance）

存放项目约束、编码规范、关键决策记录。

## 约束来源
- `AGENTS.md` 的"关键约束"与"已知行为"是最高优先级约束，勿违反。
- 新约束在此目录补充，并在 `AGENTS.md` 同步索引。

## 文档约定
- 文件名用小写 + 连字符，如 `http-client-constraint.md`。
- 每篇文档开头写明：约束内容、原因、违反后果。
